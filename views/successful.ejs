<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Report Generated</title>
<style>
    /* Reset some default styling */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Body styling */
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #e2ebf0, #b5d2e8);
        color: #333;
        padding: 20px;
    }

    /* Card styling */
    .card {
        background-color: #ffffff;
        padding: 3em;
        border-radius: 12px;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        max-width: 600px;
        text-align: center;
        transition: transform 0.2s ease;
    }

    .card:hover {
        transform: scale(1.02);
    }

    /* Title text */
    .card h1 {
        font-size: 2.2rem;
        color: #4a90e2;
        margin-bottom: 1em;
    }

    /* Success message */
    .card p {
        font-size: 1.2rem;
        color: #555;
        margin-bottom: 2em;
        line-height: 1.5;
    }

    /* Button styling */
    .btn {
        display: inline-block;
        padding: 0.8em 2em;
        font-size: 1.1rem;
        color: #fff;
        background-color: #4a90e2;
        border: none;
        border-radius: 6px;
        text-decoration: none;
        transition: background-color 0.3s ease, transform 0.3s ease;
        box-shadow: 0 5px 15px rgba(74, 144, 226, 0.4);
    }

    .btn:hover {
        background-color: #357ABD;
        transform: translateY(-2px);
    }

    .btn:active {
        background-color: #2d6ba0;
        transform: translateY(0);
    }
</style>
</head>
<body>

<div class="card">
    <h1>Success!</h1>
    <p>You have successfully generated your annual report card. Thank you for using our service to streamline your reporting needs.</p>
    <a href="/home" class="btn">Return to Home</a>
</div>

</body>
</html>
